class Compute {
    
    public int largest(int arr[], int n)
    {
        int result = arr[0];
        for (int i=1; i<n; i++) {
            result = Math.max(result, arr[i]);
        }
        return result;
    }
}
